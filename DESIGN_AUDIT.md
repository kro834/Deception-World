# デザイン監査 — 2026-09-05

## iPhone管理人カードと遷移後の操作（基点 `1d3fd5b`）

六詠のカードは最小高さ210pxに対してモバイルの行が168px、外枠が524pxで固定されていたため、次の行と重なって名前が隠れていた。760px以下では六詠の行・カード・外枠を内容に合わせて伸びる配置へ変更し、行間12pxと名前の折り返しを確保した。管理外・その他の固定枠には変更を広げていない。

遷移先の表示開始後も演出が終わるまで `data-loading` が残り、実際にbodyが `overflow:hidden`／`touch-action:none` となることを再現した。ライダー・Dream・ゼウス・アーカイブの表示開始時点でロックを解除し、残りの演出はポインター入力を遮らないよう変更した。表示後のスワイプはiPhone相当で185px、iPad相当で310px進み、遅延した先頭リセットで戻らないことを確認した。

画像先読みはfetchの後にImageで同じURLを取得する二段階処理を、ネイティブ画像リクエストとdecodeへ変更。同時要求の共有、decode失敗時の再試行をテストし、Dreamのメイン背景も遷移前の先読み対象に加えた。画像の画質は変更しておらず、実機の速度改善率は未計測。

275件のテスト、型検査、lint（既存11警告・エラー0）、ビルドが成功。ブラウザ回帰確認はカードの非重複・名前と外枠への収まり・到着直後のタッチスワイプを加えた17件すべて成功。さらに320・390・600・1024px幅でカードの重なりがないことを確認した。実機Safariは未検証。

## アニメ映画サイトへの改修（基点 `5af38ea`）

既存コードと画像を基準に、トップ、共通メニュー、人物資料、エピソード、両特設ページ、フォームアーカイブ、ドリームチャプターを監査した。新しい設定・性能値・キャラクターは追加せず、画像と日本語見出しを主役にする構成へ改修した。以下が今回の記録であり、後続の節は以前の改修履歴として残している。

| 対象 | 問題と今回の変更 |
| --- | --- |
| トップ・ポスター | 多重の装飾枠を抑え、映画名とキャッチの階層を整理。iPhoneはポスター先行、横向きiPadは左右配置。操作を2段に分けて52px高を確保し、AUTOの説明とメタ情報の下端が切れない配置にした。 |
| 本編・人物資料 | HUD風の入れ子を減らし、章見出し、16px中心の本文、12pxの補助文字を整理。ライダーの長い名称を折り返し、管理人カードの名前の省略を解消した。 |
| エピソード | iPhoneのカードを画像／本文の上下配置に揃えた。ピックアップの閉じるボタンを内部スクローラーの外へ移し、本文をスクロールしても操作できるよう修正した。 |
| 共通「＋」操作 | 長押し判定を超えた静止タップが無効になる問題を修正。スライドの準備状態とタップ成立の判定を分離し、既存の部分スライド・キャンセル動作は維持した。 |
| サイドメニュー | CSSの表示切替前にフォーカスを設定して失敗する問題を修正。表示可能なフレームで移動し、パネル自体からのShift+Tabもメニュー内で循環させた。 |
| 特設・比較 | 本文・注記の読みやすさを共通化。狭いアーカイブで隠れていたスライダーのラベルを復帰し、配信CSSのバージョンを更新。計算・値・形態順序は変更していない。 |
| ドリームチャプター | 東方Projectとのコラボ映画として、既存の竹林の画像、朱色・夜紫・生成り、明朝体と縦書きへ再構成。コラボ名を明記し、人物名は顔に重ねず画像の下へ配置した。反応しない装飾の＋は隠した。 |

### 今回の検証

Chromeのタッチエミュレーションで390×844と1376×1008の `/world`、`/dream-chapter`、`/riders/leddic`、`/managers/zeus`、`/rexonance-saga`、`/extreme-saga`、`/form-archive` の14画面を確認し、ページ全体の横はみ出しとJavaScript例外は検出されなかった。トップとDreamは1024×768でも追加確認し、ポスター周辺、ライダー、管理人、エピソードとDream人物カードを画像で確認した。iPhone／iPad実機のSafariでは未検証であり、OS固有の見え方までは保証しない。

375×667と1376×1008で、18／140／300msの静止タップ、メニューのフォーカス循環とEscape、スクロール後のエピソード閉じる操作の6確認が成功。さらに120ms長押し後、スライド距離55%で開き、20%ではキャンセルになることを両サイズで確認した（追加4確認）。Dreamでは1024px幅でポスター右に不要な空列ができるCSSの競合と、初回描画で透明度低減が効かない問題も修正。375・1024・1376px幅で再検査した3確認を合わせ、再実行用の `npm run verify:ui` は13件すべて成功した。これは起動済みの開発・プレビューサーバーとChromeが必要で、`npm test` とは別のブラウザ検証である。

`npm test` 274件、`npm run typecheck`、`npm run lint`、`npm run build` は成功。lintは既存の11警告が残りエラー0。DB接続先が未設定のローカルビルドではmigrationをスキップしている。画像原本の追加生成、常時動画、外部フォントの追加通信は行っていない。Dreamの既存画像を先読みし、重い装飾・ぼかしの重なりを減らしたが、実機の速度改善率は未計測である。

Dreamの旧「人物背景を使わない」テスト契約は今回の映画デザインへの変更に合わせて、承認済み画像の再利用とタッチを遮らない契約へ変更した。アーカイブのテストはCSS配信バージョン更新に追随させたもので、比較値の検査を緩和していない。組み立て済みソースと `source-parts` は同期している。本番公開はmainへのpushとは別に、既存の公開ジョブの結果で判定する。

基点は `3006c23`。黒・ブルー・ゴールドの既存デザインを基準に、共通の可読性と操作領域を改善した。キャラクター画像、設定値、既存の長押し処理、コンテンツ保護の仕様は変更していない。

## 確認した問題と対応

| 対象 | 監査時の問題 | 対応 |
| --- | --- | --- |
| トップと一覧 | 作品情報、個別資料の導線、コラム番号が6〜9px | 意味のあるラベルを11px以上にし、補助文字の色を調整 |
| 共通ナビゲーション | ナビゲーションが11px、閉じるボタンが40px | ナビを13px、閉じる操作領域を48pxに拡大 |
| サイドメニュー | ライダー名の省略、英語ラベルとの競合 | 日本語を折り返し、狭い画面では補助ラベルを別行に配置 |
| 人物資料 | プロフィール項目や説明ラベルが7〜9px | 11px以上の文字と行間を確保し、長い項目値の折り返しを許可 |
| エピソード | 小さい現在位置表示と強い文字間隔 | ページ番号を12px、カード補助文字の色と文字間隔を調整 |
| レクソナンス／エクスプリーム | 特設ナビのリンク高が18px、メニューボタン40px | リンクとメニューを48px高に拡大 |
| 比較・形態表示 | 比較注記が9px、形態の英語表記が8px | 注記12px、形態補助文字11〜12px、比較ヘッダーは折り返し可能に |
| スマートフォンの形態見出し | 「再設計。」の「計。」だけが独立行になる | モバイル用のサイズと行間を調整 |
| ドリームチャプター | ナビゲーションとカード補助文字が小さい | ナビ11〜12px・48px高、説明ラベル11pxへ |
| 透明度を下げる設定 | 固定ヘッダーの背景が透け続ける | 共通／特設ヘッダーに不透明背景の代替を追加 |

## 検証

Chromeの独立した一時プロファイルで `/world`、`/rexonance-saga`、`/extreme-saga`、`/riders/leddic`、`/managers/zeus`、`/dream-chapter` を390・1024・1440pxの幅で描画し、18画面でページ全体の横はみ出しがないことを確認。ヘッダー・ヒーロー・メニュー・比較・エピソード・形態表示のスクリーンショットも確認した。

320px幅では `/world`、両特設ページ、`/dream-chapter`、`/form-archive` のページ全体に横はみ出しがないことを確認した。フォームアーカイブ内の全形態を個別に監査したものではない。

メニュー開閉、Enter／Escapeでのキーボード操作、エピソードの01→02送り、比較対象の変更による値の更新、マックスへの形態切替を実際のブラウザ操作で確認した。iPhone／iPadの実機Safari検証は未実施。

`npm test` は273件成功。`npm run typecheck`、`npm run lint`、`npm run build` はすべて成功。lintは既存の11警告が残り、エラーは0。外部DATABASE_URLは未設定のためローカルビルドのDB migrationはスキップされた。

## 公開環境の問題

### メニュー・入口の統一と初期画像軽量化（2026-09-05）

サイドメニューの青いカード状背景を抑え、現在地だけを共通ガラス素材で示すよう変更。ENTER THE WORLDは共通のガラス色・細い境界線とカプセル形状に合わせ、52pxの操作高を確保した。装飾用の新しい動画・画像・常時アニメーションは追加していない。

初期ポスターは原本を保持し、1024×1536の配信用WebPを別途生成。561,774 bytesから468,454 bytesへ16.6%削減した。これは画像容量の比較であり、ページ全体の速度が同率改善したという計測ではない。WorldのHTML headで先読みし、遷移時先読み・背景・ポスター・下部表示を同じURLに統一。ChromeのResource Timingで当該画像の通信が1件であることを確認した。

390・1024・1440px幅のChromeで画面とメニューを撮影し、横はみ出しなし・開閉成功を確認。実機Safariは未検証。274件のテスト、型検査、lint（既存11警告、エラー0）、ビルドが成功した。画像容量上限と先読みURLの一致を回帰テストに追加した。

作業開始前のmain（`3006c23`）の[公開ジョブ](https://github.com/kro834/Deception-World/actions/runs/33407641678)は `Validate deployment credentials` で失敗していた。GitHub Actionsの注記はproduction環境の `VERCEL_TOKEN`、`VERCEL_ORG_ID`、`VERCEL_PROJECT_ID` が未設定と明示している。公開にはこの設定の復旧が必要であり、mainへのpushと本番公開成功は別々に確認する。
