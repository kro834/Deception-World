# Local optional. Leave blank unless you need these services.
# Do not put real secrets in git.

BETTER_AUTH_SECRET=
GROK_AUTH_CLIENT_ID=
GROK_AUTH_CLIENT_SECRET=
GROK_PREVIEW_CLIENT_SECRET=
DATABASE_URL=
VITE_PUBLIC_HOSTNAME=
VITE_AUTH_ENABLED=false
XAI_API_KEY=
# Optional. Defaults to grok-4.6 when XAI_API_KEY is configured.
XAI_CHAT_MODEL=grok-4.6
# Optional remote-inference budget units per UTC day (Normal=1, Pro=3).
ARCHIVE_AI_GLOBAL_DAILY_LIMIT=250
