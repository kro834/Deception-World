# Deception World デザイン監査・映画サイト改修

## 方針と参考

既存の画像・作品設定・ページ遷移・公開先を維持し、キービジュアルを主役とする映画サイトの構成へ更新しました。外部サイトの画像・ロゴ・コードは移植していません。

参考は [Fate/strange Fake](https://fate-strange-fake.com/) の章構成、[呪術廻戦](https://jujutsukaisen.jp/) の作品紹介・エピソード・人物の情報分離、[チェンソーマン レゼ篇](https://www.chainsawman.dog/movie_reze/) のキービジュアル切替、[葬送のフリーレン](https://frieren-anime.jp/) のビジュアルと情報の段階的な構成です。参考サイトの実機性能を比較したものではありません。

## 監査結果と変更

| 確認した問題 | 対応 |
| --- | --- |
| スマートフォンでは作品名がポスターと操作欄の後ろになる | 作品名を独立させ、作品名→ビジュアル→導入文の順に再構成 |
| ポスターの周囲に複数の枠・光・軌道が重なり、作品画像と競合する | 表示番号を上部の細いキャプションへ移し、枠と操作欄を簡素化 |
| 同じ暗いパネルが続き、各章の役割が伝わりにくい | 導入文は明るい紙面、キャラクター・エピソードは暗い画面に分け、大きな章番号と境界線を追加 |
| 共通メニューのガラス枠と文字の階層がばらつく | 行形式のメニューへ統一し、閉じるボタンとメニュー操作の48px以上の領域を維持 |
| 個別テーマの後に旧CSSが適用され、iPadで旧装飾が復活する | base→addon→映画テーマの順を明示し、個別特設ページのテーマはその後に維持 |
| ホームの自動ポスター切替がメニュー・ピックアップの裏でも進む | 各表示状態・非表示タブ・画面外・縮小モーションを確認して停止 |
| 次画像の表示準備が終わる前に自動切替する | 次の1枚を先読みし、decode成功後だけ切替。失敗時は現在の画像を維持 |
| 夢章の章ナビの長い英字が小さい画面で詰まる | 英字の専用書体・字間・折り返しを調整 |
| 夢章の低い横向き画面向け指定を後続テーマが上書きする | 最終テーマ側に低い横画面の配置・高さを定義 |

ホームと夢章には独自の章見出しの登場演出を追加しました。ホームの境界線は横へ開き、見出しは14pxだけ移動して現れます。どちらも480〜700msの一度きりの演出で、transformとopacityだけを使用します。縮小モーションや軽量表示では実行せず、非表示タブでは停止し、画面遷移時に監視とアニメーションを解除します。JavaScriptやAnimation APIが使えなくても本文を隠しません。

## 「＋」の確認範囲

ホームのコラムとライダー詳細へのスライダー、ライダーページのフォーム用スライダー、エピソードの「＋」を対象に、表示枠とボタンの矩形を比較します。コラムでは実際のタッチ入力を使って、スライド途中と元の位置へ戻った状態も検証します。はみ出しが再現しないコントロールの位置・スライドしきい値・タップ判定は変更していません。

## 検証

- `npm test`: 288件成功。表示演出の一回性、非表示タブ、縮小モーション、解除処理の実行テストを含む。
- `npm run typecheck`: 成功。
- `npm run lint`: エラー0件、既存の警告11件。
- `npm run build`: 成功。ローカルのDATABASE_URL未設定により外部DBマイグレーションはスキップ。
- `npm run verify:ui`: 35項目成功。メニューのフォーカス・Escape、コラムの短いタップ・55%スライド・中止・縦スクロール、エピソードの切替と閉じる操作、フォーム表示など。
- `node scripts/verify-film-direction.mjs`: 320×740、375×812、393×851、430×932、1024×768、1376×1008、844×390で枠内位置・文字・横はみ出しとタッチ入力を検証する専用スクリプト。

ブラウザー検証はChromeの画面サイズ・タッチ・User-Agentエミュレーションです。物理iPhone/iPad上のSafariや実機AndroidのGPU・メモリ性能まで検証したものではありません。描画処理は削減していますが、実機ベンチマークによる速度向上率は示していません。

## 公開

既存のGitHub Actions／Vercelの公開経路を維持しています。検証済みのソースをmainへ反映した後、該当コミットの公開ジョブを別途確認します。Vercelの認証情報が未設定であれば、pushの成功を本番公開の成功とは扱いません。

## 追補：タイトルからのスクロールと近未来的な文字演出

アプリ共通とフォームアーカイブにあった、documentのcapture段階で文字選択開始をキャンセルする処理を除去しました。文字から始まるブラウザーのネイティブ操作への干渉を避けるための修正です。CSSによる選択・長押しメニュー防止、およびcopy/cut/paste/contextmenu/dragstart・コピー系ショートカットの制御は維持しています。アーカイブのHTMLと読み込むスクリプトのバージョンはr45に更新し、旧処理がキャッシュに残る経路も更新しました。

ホームは細いシアン／赤の計器ラインとコーナー表現、作品情報の縦ライン、見出しの折り返しを調整しました。本文の明暗による読みやすさや既存画像・コピーは維持しています。文字の出現は520msの小さな移動と680msの光の走査に変更し、ホームの主要見出しとドリームチャプターの章見出しに適用しました。夢章は作品固有の暖色を使います。走査用の要素は読み上げ対象外・タッチ透過・絶対配置で、スクロールを捕捉せず、文字を一文字ずつ分割した大量のDOMや常時ループは追加していません。縮小モーション、軽量表示、非表示タブ、画面遷移時の停止も維持しています。

`scripts/verify-title-scroll.mjs` は375×812、430×932、1024×768、1376×1008の24条件で、ホームのh1／Story見出し、レクソナンス、エクスプリーム、夢章、サーガ詳細の文字の実座標からCDPのタッチ入力を開始し、縦方向の移動、コピー防止の維持、選択開始の非キャンセル、横はみ出しがないことを確認します。エクスプリームには既存の見出しタッチ透過があるため、その意図した親要素に入力が届くことも確認します。24条件すべて成功し、アーカイブ内の見出しも別途iPhone／iPad相当で約230pxの移動を確認しました。

ただし修正前もChromeでは文字上のスクロールが成功していたため、これらは非回帰確認であり、Safari固有症状の直接的な再現・解消証明ではありません。WebKitの実行ファイルは未導入で、物理iPhone／iPad上での検証は未実施です。

最終リリースゲートは`npm run release:build`で実行し、lintはエラー0件・既存警告11件、単体テスト290件、型チェック、ビルドが成功しました。world-homeの分割ソースは同期し、結合結果との完全一致も確認しました。

最終版で`npm run verify:ui`の35項目と`node scripts/verify-film-direction.mjs`の7画面サイズも成功しました。メニューのフォーカス、遷移直後の縦スクロール、コラムのタップ・55%スライド・中止、エピソード切替と閉じる操作、フォームの見出し／閉じるボタン、スライド途中も含む「＋」の枠内位置を含みます。
